import '../src/rux-design-token-preview.js';
describe('RuxDesignTokenPreview', () => {
    // it('has a default title "Hey there" and counter 5', async () => {
    //   const el = await fixture<RuxDesignTokenPreview>(html`<rux-design-token-preview></rux-design-token-preview>`);
    //   expect(el.title).to.equal('Hey there');
    //   expect(el.counter).to.equal(5);
    // });
    // it('increases the counter on button click', async () => {
    //   const el = await fixture<RuxDesignTokenPreview>(html`<rux-design-token-preview></rux-design-token-preview>`);
    //   el.shadowRoot!.querySelector('button')!.click();
    //   expect(el.counter).to.equal(6);
    // });
    // it('can override the title via attribute', async () => {
    //   const el = await fixture<RuxDesignTokenPreview>(html`<rux-design-token-preview title="attribute title"></rux-design-token-preview>`);
    //   expect(el.title).to.equal('attribute title');
    // });
    // it('passes the a11y audit', async () => {
    //   const el = await fixture<RuxDesignTokenPreview>(html`<rux-design-token-preview></rux-design-token-preview>`);
    //   await expect(el).shadowDom.to.be.accessible();
    // });
});
//# sourceMappingURL=rux-design-token-preview.test.js.map